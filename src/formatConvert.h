#include <string>
#include <iostream>
#include <stdlib.h>
#include <stdio.h>
#include <Windows.h>
#include <io.h>

std::string string_to_hex(const std::string& input);
std::string UTF8toANSI(const char* strUTF8);